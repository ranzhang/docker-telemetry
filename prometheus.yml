global:
  scrape_interval:    5s # By default, scrape targets every 5 seconds.

scrape_configs:
  - job_name: 'pipeline'
    static_configs:
      - targets: ['DOCKER_HOST_IP:8989']
  - job_name: 'prometheus'
    static_configs:
      - targets: ['DOCKER_HOST_IP:9091', 'DOCKER_HOST_IP:9090']

